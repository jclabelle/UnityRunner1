using UnityEngine;

namespace RunLevels.Sections
{
    public class LevelSectionV2 : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
